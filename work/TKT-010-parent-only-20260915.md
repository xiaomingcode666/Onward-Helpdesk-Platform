# TKT-010 关联类型简化（2026-09-15）

用户最新要求：取消“普通关联”和“当前问题导致对方问题”，新增关联仅保留父子关系；重复问题归类仍以首次工单为父单。

已移除页面的普通关联、因果关联选项，并在后端拒绝这两类新建请求。历史关联保留查询及按权限解除能力，不删除原始记录。

验证：定向服务及 HTTP 测试、两个关联浏览器流程和后端构建通过。本地服务已更新，未登录请求返回预期 HTTP 401。

在线需求表未更新，状态待同步。本次整理时原 work 目录下需求依据与交付说明已不可见，因此另存本记录，不重建或覆盖原文档。
