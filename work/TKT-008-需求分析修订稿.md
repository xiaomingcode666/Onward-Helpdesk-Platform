# TKT-008 需求分析修订稿

日期：2026-09-15。目标：简化需求分析，保留邮件回复正确关联和无法匹配时转人工的要求。

主要依据：[用户指定的在线需求表](https://www.kdocs.cn/l/cb6HdI2uBMOG)。本次在线访问跳转至 `account.wps.cn` 后被自动审批拒绝，尚未核对在线最新行内容。

已核对用户在本任务中贴出的原文与本地历史表 `outputs/Onward_第4章需求与系统差距对照表_优先级修订版_20260910.xlsx` 的 TKT-008 一致。本地位置为“第4章功能对照”第 24 行，“需求分析”为 D 列；此位置仅供追溯，在线写入前须重新按需求 ID 定位。

原需求：

> [MUST] Email Reply 必须关联正确 Thread；无法可靠匹配时创建待人工关联记录，不得静默附加到错误工单。

建议替换“需求分析”的内容：

> 收到客户邮件回复后，能可靠匹配的关联到原邮件会话（Thread）及对应工单；无法确认归属的，创建“待人工关联”记录，由客服确认后关联，不能猜测挂单。

供开发和验收理解的三个场景：

1. 可靠匹配到唯一目标：回复进入正确的原 Thread 及对应工单。
2. 未匹配、多个候选或匹配信息冲突：保存待人工关联记录，人工确认前不附加到任何工单。
3. 人工确认后：关联到选定的正确 Thread 及对应工单，能查到处理结果。

用户最新排期要求（2026-09-15）：TKT-008 后移到邮件自动录入工单完成并验证之后。先完成收件接入、邮件解析、自动建单、重复邮件去重和失败记录，再开发回复关联及待人工关联处理。自动建单阶段应保留邮件标识和回复引用信息，供后续关联使用。

本次记录的是 TKT-008 的需求分析简化及上述开发顺序；用户尚未指定新的 P0–P4 档位或周次，因此不将具体优先级和日期写成已确认。本轮尚未实施邮件接入开发，也不据此更新需求完成状态。

当前进度：本地修订稿及后移依赖已记录；在线内容待核对，需求分析和排期依赖尚未同步到在线表。`work/需求开发依据.md` 当前缺失，已读取 `.codex/MEMORY.md` 和用户最新 AGENTS.md 规则，不根据历史表推断在线最新状态。
