# TKT-009 通知模板代码内置简化交付说明

日期：2026-09-17。状态：开发完成，已本地验证；在线需求表状态未修改。

## 本次调整

- 删除企业端“通知模板”菜单、页面、API 和模板 CRUD/审批逻辑。
- 通知模板统一维护在 `internal/services/notification_template_defaults.go`，随代码审核和发布生效。
- 发送时按通知 code、渠道和接收人 Locale 选择代码模板，支持默认语言回退和通用模板兜底。
- 发送前继续执行敏感信息检查，命中后阻断并记录发送尝试。
- 发送结果继续写入投递记录和发送尝试表。
- 历史 `notification_templates` 表保留用于兼容旧数据，但运行时不再读取或写入。

## 验证

- 通知模板、Locale、敏感信息相关 Go 测试通过。
- Go 1.26 后端镜像构建通过。
- 前端 TypeScript 检查和 ESLint 通过。
- 后端容器重建后处于 `healthy`，`/api/health/ready` 返回 200。
- 旧模板接口 `/api/enterprise/notifications/templates` 返回 404。
- 旧模板页面 `/enterprise/notifications/templates` 返回 404。
- 通知中心 `/enterprise/notifications` 返回 200。

## 未执行

- 在线需求表需要 WPS 登录，本次未修改线上状态。
- 本轮未执行完整 CI 的 PostgreSQL/Linux 验收套件；改动影响范围已通过相关单测、镜像构建和本地运行验证。
