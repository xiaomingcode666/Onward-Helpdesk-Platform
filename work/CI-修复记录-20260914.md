# CI 检查修复记录

## 问题与范围

2026-09-14 核查：`feature/fnd-002-003-delivery` 的 CI 运行 `34796587429` 四项失败；`main` 的部署运行 `34299787562` 在 Docker Hub 凭据检查失败，两个部署任务因此跳过。提交成功仅表示代码上传，不能作为检查或交付通过的依据。

修复在独立分支 `fix/ci-checks` 中进行，基于 `5eb6704`；原工作区未提交的 TKT-004/TKT-006 修改保留。

## 修复内容

- 后端：修正 18 个 Go 文件格式；补齐 RAG 加载测试的合成夹具；清理测试数据库连接；按当前日历派单规则验证注销，并补验登录令牌撤销；隔离语音测试的外部环境变量。
- 前端：升级存在安全公告的依赖并对齐 Tiptap 版本；修复 ESLint 错误；复查并修正非法设备编号触发重渲染及手册弹窗加载状态问题。
- 验收数据：CI 生成临时管理员密码并遮蔽日志；补齐测试 AI 账户就绪信息；明确中文测试租户；改用当前企业工作日历；按供应商邀请注册流程建立测试账号。
- 浏览器用例：适配服务码入口当前默认语言和内嵌登录流程，保留匿名权限、真实身份、租户、设备及会话关联断言。
- 工程师工作台：修复客户已通过 WebSocket 上线但仍显示“客户离线”的问题，改为读取会话实时状态；断线和到期清理继续使用既有实时机制。
- 密钥扫描：对逐项核验的 18 条历史测试标识、环境变量名和模拟凭据进行精确指纹豁免；保留后续提交的扫描及原有安全检查。

## 验证记录

- Node.js 22 / pnpm 10.30.2：TypeScript、ESLint（0 错误）、SDK 与 Next.js 生产构建通过；生产依赖审计为 0 严重、0 高危、1 中危，原高危门槛通过。
- Go 1.26 系列 / CGO：普通测试通过首轮、失败项修复复验及剩余 27 项补跑完成全量执行；`go vet ./...` 通过。单次全量测试曾触发默认 10 分钟超时，CI 保留 race 检查并将测试超时设为 30 分钟。
- Go 1.26.8：漏洞扫描通过，未发现可达漏洞。
- Windows 并发回归：服务层之外的 56 个测试包通过，其中 54 个因系统 DLL 入口缺失而无法启动的包，改用本机外部链接后通过。服务层首轮完成 676 项后总耗时达到 30 分钟；补跑完成 19 项后主动停止串行进程，将剩余 37 项拆为 13/12/12 三组并行，三组均通过，无断言失败或 race 警告。逐名核对 676+19+37=732 项，遗漏、额外项、重复和跳过均为 0。未修改 Linux CI 的链接方式；这是分批执行证据，不能描述成单次全仓通过。中断的补跑仅留下覆盖率表头，因此不提供或宣称完整合并覆盖率。
- Gitleaks 8.24.2 及核查时官方最新版 8.30.1：完整历史扫描通过，0 项发现；8.30.1 发布包已与官方 SHA-256 校验文件核对。
- P0 使用独立 PostgreSQL、Redis、Qdrant、模拟 AI 服务和合成账号：最终 3 项全部通过（2.8 分钟）。覆盖客户、工程师、供应商售后闭环，断线与恢复、工单重开、知识审批与检索、AI 路由隔离和服务码登录；未跳过离线断言。随后 TypeScript、定向 ESLint 和 Next.js 生产构建再次通过。
- 会议仅模拟外部 Jitsi SDK 和 iframe，并阻止访问真实会议服务；本系统的建会、权限、参与者、入会回写和结束接口继续真实执行。此证据不证明真实音视频质量。
- 本次启动的隔离前后端、模拟 AI 进程，以及 `onward-ci-review-20260914` 专用测试容器和合成数据已清理，可通过验收夹具重新生成。日志保留在修复工作区 `tmp/`，不提交运行配置、账号、测试输出或工具下载文件。

## 远端检查状态

本地检查结果不代表 GitHub 检查已通过。首次推送 `fix/ci-checks` 被自动审批拒绝，理由是尚未核实目标仓库归属及这批代码的上传授权；该次没有上传成功。后续已只读核对：目标为现有 `origin` 对应的公开仓库 `xiaomingcode666/Onward-Helpdesk-Platform`，原远程功能分支仍指向本次修复基线 `5eb6704`。最终提交仍需推送并跟进新一轮 CI 的实际结果。

## 部署仍需配置

镜像任务需要仓库可用的 `DOCKERHUB_USERNAME` 和 `DOCKERHUB_TOKEN`。预发布和生产还需要各自的服务器地址、SSH 用户及密钥，服务器配置、项目配置包和真实健康检查域名。当前部署脚本固定使用 `staging.remotehelpdesk.com`、`app.remotehelpdesk.com`，不能认为填完镜像凭据就能交付部署。

本次不修改部署凭据、不执行真实部署、不合并到主分支。历史运行的红色结果不会因修复分支通过而自动改写；应以修复提交的实际检查结果为准。
